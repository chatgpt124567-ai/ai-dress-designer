# دليل تشغيل السيرفر المحلي

## ✅ التحديثات الأخيرة

تم نقل جميع ملفات المشروع من مجلد `ai-dress-designer` إلى جذر المستودع.
ملفات التشغيل `start-server.ps1` و `start-server.bat` تعمل الآن بشكل صحيح مع البنية الجديدة.

---

## 🚀 طرق تشغيل السيرفر

### **الطريقة 1: استخدام PowerShell Script (موصى به)**

انقر نقراً مزدوجاً على ملف:
```
start-server.ps1
```

أو من PowerShell:
```powershell
.\start-server.ps1
```

### **الطريقة 2: استخدام Batch File**

انقر نقراً مزدوجاً على ملف:
```
start-server.bat
```

### **الطريقة 3: استخدام npm مباشرة**

من PowerShell أو Command Prompt:
```bash
npm run dev
```

---

## 📋 ما يفعله السكريبت تلقائياً

1. ✅ **فحص Node.js**: يتحقق من تثبيت Node.js
2. ✅ **فحص الحزم**: يتحقق من وجود `node_modules`
3. ✅ **تثبيت تلقائي**: إذا لم تكن الحزم مثبتة، يقوم بتثبيتها تلقائياً
4. ✅ **فحص البيئة**: يتحقق من وجود ملف `.env.local`
5. ✅ **تشغيل السيرفر**: يبدأ خادم التطوير على المنفذ 3000

---

## 🔧 المتطلبات

### **1. Node.js**
- يجب أن يكون Node.js مثبتاً على جهازك
- للتحقق: `node --version`
- للتحميل: https://nodejs.org

### **2. ملف البيئة (.env.local)**

يجب إنشاء ملف `.env.local` في جذر المشروع مع المتغيرات التالية:

```env
# Supabase Configuration
NEXT_PUBLIC_SUPABASE_URL=your_supabase_project_url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key

# Google Gemini API
GOOGLE_GEMINI_API_KEY=your_google_gemini_api_key
```

---

## 🌐 الوصول إلى التطبيق

بعد تشغيل السيرفر، افتح المتصفح على:

- **الصفحة الرئيسية**: http://localhost:3000
- **صفحة التصميم**: http://localhost:3000/design
- **صفحة الملف الشخصي**: http://localhost:3000/profile
- **تسجيل الدخول**: http://localhost:3000/auth/login

---

## ⚠️ حل المشاكل الشائعة

### **المشكلة 1: "Node.js is not installed"**

**الحل:**
1. قم بتحميل Node.js من https://nodejs.org
2. ثبت Node.js
3. أعد تشغيل PowerShell/Terminal
4. جرب مرة أخرى

### **المشكلة 2: "Failed to install dependencies"**

**الحل:**
```bash
# احذف node_modules و package-lock.json
Remove-Item -Recurse -Force node_modules
Remove-Item package-lock.json

# أعد التثبيت
npm install
```

### **المشكلة 3: "Port 3000 is already in use"**

**الحل:**
```bash
# أوقف العملية التي تستخدم المنفذ 3000
# أو استخدم منفذ آخر
npm run dev -- -p 3001
```

### **المشكلة 4: ".env.local file not found"**

**الحل:**
1. أنشئ ملف `.env.local` في جذر المشروع
2. أضف المتغيرات المطلوبة (انظر أعلاه)
3. احفظ الملف
4. أعد تشغيل السيرفر

### **المشكلة 5: "Cannot find module 'next'"**

**الحل:**
```bash
# تأكد من أنك في المجلد الصحيح
cd "c:\Users\khale\OneDrive\سطح المكتب\ai designer virsion 2"

# أعد تثبيت الحزم
npm install
```

---

## 🛑 إيقاف السيرفر

لإيقاف السيرفر، اضغط:
```
Ctrl + C
```

ثم أكد بالضغط على `Y` أو `Enter`

---

## 📝 ملاحظات مهمة

1. **المجلد الصحيح**: تأكد من أنك في جذر المشروع عند تشغيل السكريبت
2. **الحزم المثبتة**: `node_modules` يجب أن يكون في جذر المشروع (نفس مستوى `package.json`)
3. **ملف البيئة**: `.env.local` يجب أن يكون في جذر المشروع
4. **المنفذ الافتراضي**: السيرفر يعمل على المنفذ 3000 افتراضياً

---

## 🔄 التحديثات الأخيرة

### ما تم تغييره:
- ✅ نقل جميع ملفات المشروع من `ai-dress-designer/` إلى الجذر
- ✅ تثبيت `node_modules` في الجذر
- ✅ تحديث `.gitignore` لاستبعاد المجلد القديم
- ✅ إضافة `vercel.json` للنشر الصحيح على Vercel

### ما لم يتغير:
- ✅ ملفات `start-server.ps1` و `start-server.bat` تعمل كما هي
- ✅ جميع الأوامر تعمل من جذر المشروع مباشرة
- ✅ لا حاجة لتغيير أي شيء في طريقة التشغيل

---

## 🎯 الخلاصة

**للتشغيل السريع:**
1. انقر نقراً مزدوجاً على `start-server.ps1` أو `start-server.bat`
2. انتظر حتى يبدأ السيرفر
3. افتح http://localhost:3000 في المتصفح

**هذا كل شيء! 🎉**

