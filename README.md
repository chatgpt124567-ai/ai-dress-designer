# AI Dress Designer - مصمم الفساتين بالذكاء الاصطناعي

تطبيق ويب كامل (Full-Stack) يستخدم الذكاء الاصطناعي لتصميم الفساتين بناءً على وصف نصي من المستخدم.

## 🌟 المميزات

- **وصف بسيط**: اكتب وصفاً بسيطاً للفستان الذي تريده بلغتك الطبيعية
- **تحسين تلقائي**: يستخدم Google Gemini لتحويل الوصف إلى برومبت احترافي ومفصل
- **توليد الصور**: يستخدم Google Gemini Flash لتوليد صورة عالية الجودة للتصميم
- **واجهة عربية**: دعم كامل للغة العربية مع تصميم RTL
- **تصميم متجاوب**: يعمل بشكل مثالي على جميع الأجهزة (Mobile-First)
- **مفتاح واحد فقط**: يستخدم Google Gemini API فقط - لا حاجة لمفاتيح متعددة!

## 🛠️ التقنيات المستخدمة

- **Framework**: Next.js 16 (App Router)
- **Language**: TypeScript
- **Styling**: Tailwind CSS
- **AI API**:
  - Google Gemini API (gemini-2.5-flash) - لتحسين البرومبت
  - Google Gemini API (gemini-2.0-flash-exp-image-generation) - لتوليد صور الفساتين

## 📋 المتطلبات

- Node.js 18+
- npm أو yarn أو pnpm
- مفتاح Google Gemini API

## 🚀 التثبيت والتشغيل

### 1. تثبيت المكتبات

```bash
npm install
```

### 2. إعداد متغيرات البيئة

أنشئ ملف `.env.local` في المجلد الرئيسي وأضف المفتاح التالي:

```env
# Google Gemini API Key
# Used for both prompt enhancement and image generation
GOOGLE_GEMINI_API_KEY=your_google_gemini_api_key_here
```

#### الحصول على المفتاح:

- **Google Gemini API Key**: [https://makersuite.google.com/app/apikey](https://makersuite.google.com/app/apikey)
  - سجل حساب Google
  - اذهب إلى Google AI Studio
  - أنشئ مفتاح API جديد

### 3. تشغيل التطبيق

**الطريقة 1: استخدام npm**
```bash
npm run dev
```

**الطريقة 2: استخدام ملفات التشغيل السريع**
- **Windows CMD**: انقر مرتين على `start-server.bat`
- **PowerShell**: `.\start-server.ps1`

افتح المتصفح على [http://localhost:3000](http://localhost:3000)

## 📁 هيكل المشروع

```
ai-dress-designer/
├── app/
│   ├── api/
│   │   ├── enhance-prompt/
│   │   │   └── route.ts          # API لتحسين البرومبت
│   │   └── generate-image/
│   │       └── route.ts          # API لتوليد الصور
│   ├── design/
│   │   └── page.tsx              # صفحة التصميم
│   ├── globals.css               # الأنماط العامة
│   ├── layout.tsx                # Layout الرئيسي
│   └── page.tsx                  # الصفحة الرئيسية
├── types/
│   └── index.ts                  # تعريفات TypeScript
├── .env.local                    # متغيرات البيئة (غير مرفوع على Git)
├── .env.example                  # مثال لمتغيرات البيئة
└── package.json
```

## 🎯 كيفية الاستخدام

1. افتح التطبيق وانقر على "ابدأ التصميم الآن"
2. اكتب وصفاً مفصلاً للفستان الذي تريده
3. انقر على "صمم الفستان"
4. انتظر بضع ثوانٍ حتى يتم:
   - تحسين الوصف باستخدام Gemini 2.5 Flash Lite (سريع)
   - توليد الصورة باستخدام Gemini 2.0 Flash Experimental Image Generation (مجاني)
5. شاهد التصميم المولّد وقم بتحميله إذا أعجبك

**ملاحظة**: إذا ظهرت رسالة "الخدمة مزدحمة"، انتظر 30-60 ثانية وحاول مرة أخرى.

## 💡 نصائح للحصول على أفضل النتائج

- **كن محدداً**: اذكر اللون، نوع القماش، الطول، والمناسبة
- **صف الأسلوب**: حدد الأسلوب (كلاسيكي، عصري، رومانسي، إلخ)
- **التفاصيل الزخرفية**: اذكر التطريز، الخرز، الدانتيل، أو أي زخارف
- **المناسبة**: حدد المناسبة (زفاف، سهرة، كوكتيل، يومي)

## 🔧 البناء للإنتاج

```bash
npm run build
npm start
```

## 📝 ملاحظات مهمة

- تأكد من إضافة ملف `.env.local` إلى `.gitignore` لحماية مفتاح API
- Google Gemini API مجاني حتى حد معين، راجع [التسعير](https://ai.google.dev/pricing)
- النماذج المستخدمة:
  - `gemini-2.5-flash` لتحسين البرومبت (نصوص)
  - `gemini-2.0-flash-exp-image-generation` لتوليد الصور (متاح في الخطة المجانية)
- **نظام إعادة المحاولة**: التطبيق يحاول تلقائياً 3 مرات عند فشل الطلب
- **ملاحظة**: إذا واجهت خطأ 503 (Service Overloaded)، انتظر دقيقة وحاول مرة أخرى

## 📄 الترخيص

هذا المشروع مفتوح المصدر ومتاح للاستخدام الشخصي والتعليمي.

## 🤝 المساهمة

المساهمات مرحب بها! لا تتردد في فتح Issue أو Pull Request.

---

صُنع بـ ❤️ باستخدام Next.js و Google Gemini
