# كيفية تشغيل التطبيق - How to Run

## 🚀 طرق تشغيل السيرفر

لديك **3 طرق** لتشغيل التطبيق:

---

## 1️⃣ الطريقة الأولى: ملف BAT (الأسهل) ⭐

### الخطوات:
1. انقر نقراً مزدوجاً على ملف **`start-server.bat`**
2. سيفتح نافذة Command Prompt
3. سيتحقق من المتطلبات تلقائياً
4. سيشغل السيرفر على http://localhost:3000

### المميزات:
- ✅ سهل جداً - نقرة واحدة
- ✅ يتحقق من Node.js تلقائياً
- ✅ يثبت المكتبات إذا لم تكن مثبتة
- ✅ يعرض رسائل واضحة بالعربية

---

## 2️⃣ الطريقة الثانية: ملف PowerShell (مع فتح المتصفح تلقائياً)

### الخطوات:
1. انقر بزر الماوس الأيمن على ملف **`start-server.ps1`**
2. اختر **"Run with PowerShell"**
3. سيشغل السيرفر ويفتح المتصفح تلقائياً بعد 5 ثوانٍ

### المميزات:
- ✅ يفتح المتصفح تلقائياً
- ✅ واجهة ملونة وجميلة
- ✅ يتحقق من جميع المتطلبات

### ملاحظة:
إذا ظهرت رسالة خطأ حول Execution Policy:
```powershell
Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser
```

---

## 3️⃣ الطريقة الثالثة: Terminal (للمطورين)

### الخطوات:
1. افتح Terminal في مجلد المشروع
2. شغّل الأمر:
```bash
npm run dev
```

### المميزات:
- ✅ طريقة المطورين التقليدية
- ✅ تحكم كامل

---

## 🖥️ إنشاء اختصار على سطح المكتب

### الخطوات:
1. انقر نقراً مزدوجاً على **`create-desktop-shortcut.bat`**
2. سيتم إنشاء اختصار على سطح المكتب
3. يمكنك الآن تشغيل التطبيق من سطح المكتب!

### المميزات:
- ✅ وصول سريع من سطح المكتب
- ✅ لا حاجة للبحث عن المجلد

---

## 📋 متطلبات التشغيل

قبل تشغيل التطبيق، تأكد من:

### 1. Node.js مثبت
- تحقق بالأمر: `node --version`
- إذا لم يكن مثبتاً: [تحميل Node.js](https://nodejs.org)

### 2. المكتبات مثبتة
- سيتم التثبيت تلقائياً عند أول تشغيل
- أو شغّل يدوياً: `npm install`

### 3. ملف `.env.local` موجود
- يجب أن يحتوي على:
```env
GOOGLE_GEMINI_API_KEY=your_key_here
```

---

## 🌐 الوصول إلى التطبيق

بعد تشغيل السيرفر:

### الصفحة الرئيسية:
```
http://localhost:3000
```

### صفحة التصميم مباشرة:
```
http://localhost:3000/design
```

---

## ⚠️ حل المشاكل الشائعة

### المشكلة: "Port 3000 already in use"
**الحل**:
```bash
# أوقف العملية التي تستخدم المنفذ
netstat -ano | findstr :3000
taskkill /PID <PID> /F
```

### المشكلة: "Node.js is not recognized"
**الحل**:
- ثبّت Node.js من https://nodejs.org
- أعد تشغيل الكمبيوتر

### المشكلة: "Cannot find module"
**الحل**:
```bash
# احذف node_modules وأعد التثبيت
rmdir /s /q node_modules
npm install
```

### المشكلة: "API Key error"
**الحل**:
1. تحقق من ملف `.env.local`
2. تأكد من صحة المفاتيح
3. أعد تشغيل السيرفر (Ctrl+C ثم شغّل مرة أخرى)

---

## 🛑 إيقاف السيرفر

### في Command Prompt أو PowerShell:
- اضغط **`Ctrl + C`**
- أو أغلق النافذة

### في Terminal:
- اضغط **`Ctrl + C`**

---

## 💡 نصائح

### للتطوير:
- استخدم `npm run dev` - يدعم Hot Reload
- التغييرات تظهر تلقائياً بدون إعادة تشغيل

### للإنتاج:
```bash
npm run build
npm start
```

### لفحص الأخطاء:
```bash
npm run lint
```

---

## 📊 مقارنة الطرق

| الطريقة | السهولة | فتح المتصفح | التحقق التلقائي |
|---------|---------|-------------|-----------------|
| BAT | ⭐⭐⭐⭐⭐ | ❌ | ✅ |
| PowerShell | ⭐⭐⭐⭐ | ✅ | ✅ |
| Terminal | ⭐⭐⭐ | ❌ | ❌ |

---

## 🎯 التوصية

**للمستخدمين العاديين**: استخدم **`start-server.bat`** (الأسهل)

**للمطورين**: استخدم **Terminal** مع `npm run dev`

**للراحة القصوى**: أنشئ اختصار على سطح المكتب!

---

## 📚 موارد إضافية

- **`README.md`** - دليل شامل للمشروع
- **`QUICKSTART.md`** - بدء سريع
- **`TROUBLESHOOTING.md`** - حل المشاكل
- **`ENV_SETUP_COMPLETE.md`** - إعداد المفاتيح

---

**استمتع بتصميم الفساتين! 🎨✨**

