# استكشاف الأخطاء وإصلاحها - Troubleshooting

## 🐛 المشاكل الشائعة والحلول

### 1. خطأ: Module not found: Can't resolve './chatkit/chatkit.mjs'

#### الوصف:
```
Module not found: Can't resolve './chatkit/chatkit.mjs'
./node_modules/openai/resources/beta/beta.mjs:8:1
```

#### السبب:
- هذا خطأ معروف في إصدار OpenAI 6.9.0
- الإصدار يفتقد وحدة ChatKit المطلوبة
- عدم توافق مع Next.js 16 و Turbopack

#### الحل:
استخدم إصدار OpenAI 4.73.1 بدلاً من 6.9.0:

```bash
# إزالة الإصدار القديم
npm uninstall openai

# تثبيت الإصدار المستقر
npm install openai@4.73.1
```

#### التحقق من الحل:
```bash
# تحقق من الإصدار المثبت
npm list openai

# شغّل التطبيق
npm run dev
```

---

### 2. خطأ: API Key غير موجود

#### الوصف:
```
Error: Missing API Key
```

#### السبب:
- ملف `.env.local` غير موجود
- أسماء المتغيرات غير صحيحة
- لم يتم إعادة تشغيل السيرفر بعد إضافة المفاتيح

#### الحل:
1. تأكد من وجود ملف `.env.local` في المجلد الرئيسي
2. تحقق من أسماء المتغيرات:
```env
OPENAI_API_KEY=sk-proj-...
GOOGLE_GEMINI_API_KEY=AIzaSy...
```
3. أعد تشغيل السيرفر:
```bash
# أوقف السيرفر (Ctrl+C)
# ثم شغّله مرة أخرى
npm run dev
```

---

### 3. خطأ: Failed to generate image

#### الوصف:
```
فشل في توليد الصورة. حاول مرة أخرى.
```

#### الأسباب المحتملة:
1. مفتاح Gemini API غير صحيح
2. تجاوز حد الاستخدام المجاني
3. مشكلة في الاتصال بالإنترنت
4. البرومبت يحتوي على محتوى محظور

#### الحلول:
1. تحقق من صحة مفتاح Gemini API
2. راجع حد الاستخدام في [Google AI Studio](https://makersuite.google.com)
3. تحقق من الاتصال بالإنترنت
4. جرب وصفاً مختلفاً

---

### 4. خطأ: Failed to enhance prompt

#### الوصف:
```
فشل في تحسين الوصف. حاول مرة أخرى.
```

#### الأسباب المحتملة:
1. مفتاح OpenAI API غير صحيح
2. نفاد الرصيد في حساب OpenAI
3. مشكلة في الاتصال بالإنترنت

#### الحلول:
1. تحقق من صحة مفتاح OpenAI API
2. راجع الرصيد في [OpenAI Platform](https://platform.openai.com/usage)
3. تحقق من الاتصال بالإنترنت

---

### 5. خطأ: Build failed

#### الوصف:
```
Error: Build failed
```

#### الحلول:
1. تحقق من عدم وجود أخطاء TypeScript:
```bash
npm run lint
```

2. امسح cache وأعد البناء:
```bash
rm -rf .next
npm run build
```

3. تحقق من تثبيت جميع المكتبات:
```bash
npm install
```

---

### 6. التطبيق بطيء في توليد الصور

#### السبب:
- توليد الصور يستغرق وقتاً (10-30 ثانية)
- هذا طبيعي ويعتمد على:
  - سرعة API
  - تعقيد البرومبت
  - حمل الخوادم

#### نصائح:
- انتظر بصبر حتى تظهر الصورة
- لا تنقر على الزر مرة أخرى أثناء المعالجة
- تحقق من مؤشر التقدم

---

### 7. الصورة المولدة لا تطابق الوصف

#### الحلول:
1. اكتب وصفاً أكثر تفصيلاً
2. استخدم الأمثلة في `EXAMPLES.md`
3. أضف تفاصيل محددة:
   - اللون الدقيق
   - نوع القماش
   - الطول
   - المناسبة
   - التفاصيل الزخرفية

---

### 8. خطأ: Port 3000 already in use

#### الوصف:
```
Error: Port 3000 is already in use
```

#### الحل:
1. أوقف العملية التي تستخدم المنفذ:
```bash
# Windows
netstat -ano | findstr :3000
taskkill /PID <PID> /F

# أو استخدم منفذ آخر
npm run dev -- -p 3001
```

---

### 9. خطأ: CORS Error

#### الوصف:
```
Access to fetch has been blocked by CORS policy
```

#### السبب:
- هذا لا يجب أن يحدث في التطوير المحلي
- قد يحدث في الإنتاج

#### الحل:
- تأكد من استخدام API Routes في Next.js
- لا تستدعي OpenAI أو Gemini مباشرة من المتصفح

---

## 🔍 تشخيص المشاكل

### فحص الـ Logs
1. افتح Developer Console في المتصفح (F12)
2. راجع تبويب Console للأخطاء
3. راجع تبويب Network لطلبات API

### فحص السيرفر
1. راجع Terminal الذي يشغل `npm run dev`
2. ابحث عن رسائل الأخطاء
3. تحقق من أن السيرفر يعمل على المنفذ الصحيح

---

## 📞 الحصول على المساعدة

إذا واجهت مشكلة غير مذكورة هنا:

1. راجع الملفات التالية:
   - `README.md` - دليل عام
   - `SETUP_GUIDE.md` - دليل الإعداد
   - `EXAMPLES.md` - أمثلة الاستخدام

2. تحقق من:
   - إصدارات المكتبات في `package.json`
   - متغيرات البيئة في `.env.local`
   - Logs في Terminal والمتصفح

3. جرب:
   - إعادة تشغيل السيرفر
   - مسح cache (`.next` folder)
   - إعادة تثبيت المكتبات (`npm install`)

---

## ✅ Checklist للتشخيص

- [ ] Node.js 18+ مثبت
- [ ] جميع المكتبات مثبتة (`npm install`)
- [ ] ملف `.env.local` موجود
- [ ] مفاتيح API صحيحة
- [ ] السيرفر يعمل (`npm run dev`)
- [ ] لا توجد أخطاء في Console
- [ ] المنفذ 3000 متاح
- [ ] الاتصال بالإنترنت يعمل

---

**إذا استمرت المشكلة، راجع الوثائق الرسمية:**
- [Next.js Docs](https://nextjs.org/docs)
- [OpenAI API Docs](https://platform.openai.com/docs)
- [Google Gemini Docs](https://ai.google.dev/docs)

