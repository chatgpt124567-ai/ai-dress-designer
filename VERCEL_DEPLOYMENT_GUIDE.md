# دليل نشر المشروع على Vercel

## 🔧 حل مشكلة 404: NOT_FOUND

إذا كنت تواجه خطأ 404 عند زيارة الموقع على Vercel، اتبع الخطوات التالية:

---

## ✅ الخطوة 1: حذف المشروع الحالي من Vercel وإعادة إنشائه

### لماذا؟
المشكلة قد تكون بسبب إعدادات خاطئة في النشر الأول.

### كيف؟
1. اذهب إلى [Vercel Dashboard](https://vercel.com/dashboard)
2. اختر المشروع **ai-dress-designer**
3. اذهب إلى **Settings** (الإعدادات)
4. انزل إلى الأسفل واضغط على **Delete Project**
5. أكد الحذف

---

## ✅ الخطوة 2: إنشاء مشروع جديد على Vercel

1. اذهب إلى [Vercel Dashboard](https://vercel.com/dashboard)
2. اضغط على **Add New** → **Project**
3. اختر **Import Git Repository**
4. ابحث عن مستودع **chatgpt124567-ai/ai-dress-designer**
5. اضغط على **Import**

### ⚠️ إعدادات مهمة جداً:

في صفحة إعداد المشروع، تأكد من:

#### **Framework Preset:**
- اختر: **Next.js**

#### **Root Directory:**
- اتركه فارغاً أو اكتب: `./`
- ⚠️ **لا تكتب** `ai-dress-designer`

#### **Build Command:**
- يجب أن يكون: `npm run build` (تلقائي)

#### **Output Directory:**
- يجب أن يكون: `.next` (تلقائي)

#### **Install Command:**
- يجب أن يكون: `npm install` (تلقائي)

---

## ✅ الخطوة 3: إضافة متغيرات البيئة (Environment Variables)

**⚠️ هذه الخطوة ضرورية جداً!**

قبل الضغط على **Deploy**، اضغط على **Environment Variables** وأضف:

### المتغيرات المطلوبة:

```
NEXT_PUBLIC_SUPABASE_URL=your_supabase_project_url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key
GOOGLE_GEMINI_API_KEY=your_google_gemini_api_key
```

### كيفية الحصول على القيم:

#### 1. **Supabase URL & Anon Key:**
- اذهب إلى [Supabase Dashboard](https://supabase.com/dashboard)
- اختر مشروعك
- اذهب إلى **Settings** → **API**
- انسخ:
  - **Project URL** → ضعه في `NEXT_PUBLIC_SUPABASE_URL`
  - **anon public** key → ضعه في `NEXT_PUBLIC_SUPABASE_ANON_KEY`

#### 2. **Google Gemini API Key:**
- اذهب إلى [Google AI Studio](https://aistudio.google.com/app/apikey)
- أنشئ API Key جديد أو استخدم موجود
- انسخه وضعه في `GOOGLE_GEMINI_API_KEY`

---

## ✅ الخطوة 4: النشر

1. بعد إضافة جميع المتغيرات، اضغط على **Deploy**
2. انتظر حتى يكتمل البناء (Build)
3. بعد اكتمال النشر، اضغط على **Visit** لزيارة الموقع

---

## 🔍 التحقق من نجاح النشر

### علامات النجاح:
- ✅ Build Status: **Ready**
- ✅ عند زيارة الموقع، يظهر الصفحة الرئيسية
- ✅ لا توجد أخطاء 404

### إذا استمرت المشكلة:

#### تحقق من Logs:
1. في Vercel Dashboard، اذهب إلى المشروع
2. اضغط على آخر Deployment
3. اضغط على **View Function Logs** أو **Build Logs**
4. ابحث عن أي أخطاء

#### الأخطاء الشائعة:

**1. خطأ في Build:**
```
Error: Cannot find module 'next'
```
**الحل:** تأكد من أن `package.json` موجود في جذر المشروع

**2. خطأ 404:**
```
404: NOT_FOUND
```
**الحل:** تأكد من أن Root Directory فارغ أو `./`

**3. خطأ في Environment Variables:**
```
Error: Missing environment variable
```
**الحل:** تأكد من إضافة جميع المتغيرات المطلوبة

---

## 📝 ملاحظات إضافية

### إعادة النشر بعد تغييرات:
- أي تغيير تدفعه إلى GitHub سيؤدي إلى إعادة نشر تلقائية على Vercel

### تغيير Domain:
- يمكنك تغيير اسم النطاق من **Settings** → **Domains**

### إضافة Custom Domain:
- يمكنك ربط نطاق خاص بك من **Settings** → **Domains** → **Add**

---

## 🆘 إذا استمرت المشكلة

إذا جربت كل الخطوات أعلاه ولا تزال المشكلة موجودة:

1. **تحقق من Build Logs** في Vercel
2. **تحقق من Function Logs** للبحث عن أخطاء Runtime
3. **تأكد من أن المشروع يعمل محلياً:**
   ```bash
   npm install
   npm run build
   npm start
   ```
4. إذا كان يعمل محلياً ولا يعمل على Vercel، المشكلة في الإعدادات

---

## ✅ Checklist النهائي

قبل النشر، تأكد من:

- [ ] المشروع في جذر المستودع (ليس في مجلد فرعي)
- [ ] ملف `package.json` موجود في الجذر
- [ ] ملف `next.config.ts` موجود في الجذر
- [ ] مجلد `app` موجود في الجذر
- [ ] Root Directory في Vercel فارغ أو `./`
- [ ] Framework Preset = Next.js
- [ ] جميع Environment Variables مضافة
- [ ] Build Command = `npm run build`
- [ ] Output Directory = `.next`

---

**حظاً موفقاً! 🚀**

