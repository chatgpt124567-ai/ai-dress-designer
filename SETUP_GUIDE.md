# دليل الإعداد - Setup Guide

## الحصول على مفاتيح API

### 1. مفتاح OpenAI API

#### الخطوات:

1. اذهب إلى [OpenAI Platform](https://platform.openai.com/)
2. قم بإنشاء حساب أو تسجيل الدخول
3. اذهب إلى [API Keys](https://platform.openai.com/api-keys)
4. انقر على "Create new secret key"
5. انسخ المفتاح وضعه في ملف `.env.local`

#### ملاحظات:
- ستحتاج إلى إضافة رصيد إلى حسابك لاستخدام API
- النموذج المستخدم: `gpt-4o-mini` (أرخص وأسرع من GPT-4)
- التكلفة التقريبية: $0.15 لكل مليون توكن إدخال

### 2. مفتاح Google Gemini API

#### الخطوات:

1. اذهب إلى [Google AI Studio](https://makersuite.google.com/app/apikey)
2. قم بتسجيل الدخول بحساب Google
3. انقر على "Get API Key" أو "Create API Key"
4. اختر مشروع Google Cloud أو أنشئ مشروعاً جديداً
5. انسخ المفتاح وضعه في ملف `.env.local`

#### ملاحظات:
- Gemini API لديه حصة مجانية سخية
- النموذج المستخدم: `gemini-2.0-flash-exp`
- قد تحتاج إلى تفعيل Generative AI API في Google Cloud Console

### 3. إعداد ملف .env.local

أنشئ ملف `.env.local` في المجلد الرئيسي للمشروع:

```env
# OpenAI API Key
OPENAI_API_KEY=sk-proj-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# Google Gemini API Key
GOOGLE_GEMINI_API_KEY=AIzaSyxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
```

**مهم جداً**: لا تشارك هذه المفاتيح مع أحد ولا ترفعها على GitHub!

## استكشاف الأخطاء

### خطأ: "مفتاح OpenAI API غير موجود"

**الحل**:
1. تأكد من وجود ملف `.env.local` في المجلد الرئيسي
2. تأكد من أن اسم المتغير صحيح: `OPENAI_API_KEY`
3. أعد تشغيل السيرفر بعد إضافة المفتاح

### خطأ: "مفتاح Google Gemini API غير موجود"

**الحل**:
1. تأكد من وجود ملف `.env.local` في المجلد الرئيسي
2. تأكد من أن اسم المتغير صحيح: `GOOGLE_GEMINI_API_KEY`
3. أعد تشغيل السيرفر بعد إضافة المفتاح

### خطأ: "النموذج غير متاح حالياً"

**الحل**:
1. تحقق من أن نموذج Gemini المستخدم متاح
2. يمكنك تغيير النموذج في `app/api/generate-image/route.ts`
3. النماذج البديلة:
   - `gemini-2.0-flash-exp`
   - `gemini-1.5-flash`
   - `gemini-1.5-pro`

### خطأ: "تم تجاوز حد الاستخدام"

**الحل**:
1. تحقق من رصيد حسابك في OpenAI
2. تحقق من حصة الاستخدام في Google Cloud Console
3. انتظر حتى يتم تجديد الحصة (عادة شهرياً)

## نصائح للتطوير

### تشغيل التطبيق في وضع التطوير

```bash
npm run dev
```

### بناء التطبيق للإنتاج

```bash
npm run build
npm start
```

### فحص الأخطاء

```bash
npm run lint
```

## الأمان

1. **لا ترفع ملف `.env.local` على GitHub**
2. أضف `.env.local` إلى `.gitignore` (تم إضافته تلقائياً)
3. استخدم متغيرات البيئة في Vercel أو أي منصة استضافة أخرى
4. راقب استخدام API لتجنب التكاليف غير المتوقعة

## الدعم

إذا واجهت أي مشاكل:
1. تحقق من console المتصفح للأخطاء
2. تحقق من terminal السيرفر للأخطاء
3. راجع [OpenAI Documentation](https://platform.openai.com/docs)
4. راجع [Google Gemini Documentation](https://ai.google.dev/docs)

---

حظاً موفقاً! 🚀

